# SPDX-License-Identifier: Apache-2.0
#
# SPDX-FileCopyrightText: © 2025 Tenstorrent AI ULC

# Development Dockerfile for TT Studio Frontend
# Optimized for hot reloading and development workflow
FROM node:22-alpine3.20

WORKDIR /app

# Copy package files first for better Docker layer caching
COPY package*.json ./

# Install dependencies inside the container image
RUN npm install

# Copy the rest of the source code
COPY . .

# Expose the port the Vite dev server will run on
EXPOSE 3000

# The default command to start the dev server
CMD ["npm", "run", "dev"]