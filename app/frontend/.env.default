# Frontend environment template. Copy to `.env` and adjust as needed.
# Auto-populated by `python3 run.py --setup` for the dev compose mode.
# Note: most VITE_* feature flags live in app/.env (used as build args).

# Backend API URL used inside the container network
VITE_BACKEND_URL=http://tt-studio-backend-api:8000
