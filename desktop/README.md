# TT-Studio Desktop

Tauri v2 desktop shell for TT-Studio. A small bundled launcher UI that
navigates the app window to a running TT-Studio stack at
`http://localhost:3000`.

Work in progress — see the repo root for the web deployment, which is
unaffected by this directory.
